name: tekartik_core
description: Core using new versioning
version: 0.1.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ">=6.0.0"
  test: ">=1.25.6"
resolution: workspace
